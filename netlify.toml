[build]
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  
# Include nodemailer in the function bundle
[functions."send-email"]
  external_node_modules = ["nodemailer"]
